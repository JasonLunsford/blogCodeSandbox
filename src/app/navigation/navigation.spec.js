
describe( 'navigation section', function() {
  beforeEach( module( 'blogCodeSandbox.navigation' ) );

  it( 'should have a dummy test', inject( function() {
    expect( true ).toBeTruthy();
  }));
});

